const Reservations = () => {
  const componentName = 'RESERVATIONS';

  return (
    <div
      className={`${componentName}_MAIN_CONTAINER h-[70dvh] overflow-hidden relative`}
    >Reservations</div>
  );
};

export default Reservations;
